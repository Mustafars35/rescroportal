import Link from "next/link"; import { Card } from "@/components/ui/card"; import { LegacyShell } from "@/components/legacy-shell"; import { RiskBadge, StageBadge } from "@/components/factory-ui"; import { orders } from "@/lib/orders";
export default function DelayedRisk(){const list=orders.filter(order=>order.risk!=="Normal");return <LegacyShell><section className="orders-heading"><div><h2>Delayed & Risk Orders</h2><p>Mock age rules are ready to be connected to real due-date logic.</p></div></section><Card className="simple-list">{list.map(order=><Link href={`/orders/${encodeURIComponent(order.id)}`} target="_blank" key={order.id}><strong>{order.id}</strong><span>{order.customer}</span><StageBadge stage={order.stage}/><RiskBadge risk={order.risk}/><b>{order.eta}</b></Link>)}</Card></LegacyShell>}
