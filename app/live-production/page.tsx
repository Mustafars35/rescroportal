import { Activity, CheckCircle2, CircleDot, Factory, Gauge } from "lucide-react";
import { Card } from "@/components/ui/card";
import { LegacyShell } from "@/components/legacy-shell";
import { Flow } from "@/components/factory-ui";
import { orders } from "@/lib/orders";
export default function LiveProduction(){const inProduction=orders.filter(order=>!['Finished','Packed'].includes(order.stage)).length;const completed=orders.filter(order=>order.stage==='Finished').length;return <LegacyShell><section className="page-heading"><span>LIVE PRODUCTION</span><h1>Factory status</h1><p>Current work in progress across all production stations.</p></section><section className="live-summary"><Card><Factory/><div><span>Active stations</span><strong>8 / 8</strong><small>All stations operational</small></div></Card><Card><Activity/><div><span>In production</span><strong>{inProduction}</strong><small>Orders currently moving</small></div></Card><Card><CheckCircle2/><div><span>Completed orders</span><strong>{completed}</strong><small>Finished in the demo flow</small></div></Card><Card><Gauge/><div><span>Daily progress</span><strong>74%</strong><small>164 of 220 planned</small></div></Card></section><Card className="flow-card live-production-card"><div className="section-heading"><div><h2>ACTIVE PRODUCTION FLOW</h2><p>Click a station to view its orders on the Dashboard.</p></div><CircleDot/></div><Flow/></Card></LegacyShell>}
